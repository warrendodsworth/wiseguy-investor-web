/**
 * firestore node server
 */
// const app = express();
// app.use(cors({ origin: true }));

// /**
//  * test webhook
//  */
// app.post('/webhook', async (req, res) => {});

// export const expressApp = functions.https.onRequest(app);
